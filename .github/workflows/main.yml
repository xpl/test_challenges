name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: SegmentStream
      run: |
        cd SegmentStream/redis-caching-proxy
        make test
